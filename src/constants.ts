export class Constants {
  static readonly DATE_FMT = 'MM-dd-yyyy';
  static readonly DATE_TIME_FMT = `${Constants.DATE_FMT} HH:mm:ss`;
  // static readonly DATE_TIME_FMT_MIL = `${Constants.DATE_FMT} @ HH:mm:ss`;
}
